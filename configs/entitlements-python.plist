<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened-runtime entitlements for the bundled CPython interpreter
         (spawned by the helper to run the transcription daemon). It records
         the mic, and loads ~hundreds of pip-installed extension modules —
         all re-signed with our Developer ID at build time, but library
         validation is disabled defensively so a wheel with unusual linkage
         can't brick the daemon. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
